
import 'dart:ui';

class AppColors {
  AppColors._(); // this basically makes it so you can't instantiate this class

  static const primaryColor = Color(0xFF3858cd);
  static const orangeColor = Color(0xFFfe932a);
}